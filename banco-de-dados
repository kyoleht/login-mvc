CREATE DATABASE login_mvc;

use login_mvc;

CREATE TABLE usuarios (
    id_usuario int(11) AUTO_INCREMENT primary key,
    nome varchar(30),
    telefone varchar(30),
    email varchar(40),
    senha varchar(32)
)   

